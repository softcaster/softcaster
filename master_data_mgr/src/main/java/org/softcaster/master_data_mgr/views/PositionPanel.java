/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/GUIForms/JPanel.java to edit this template
 */
package org.softcaster.master_data_mgr.views;

import java.awt.event.ActionEvent;
import java.util.ArrayList;
import java.util.List;
import javax.swing.JOptionPane;
import org.softcaster.commons.utils.LoggerMgr;
import org.softcaster.easy_pricer_core.data.PositionMasterData;
import org.softcaster.master_data_mgr.JMasterDataMgr;
import org.softcaster.master_data_mgr.MasterDataFacade;
import org.softcaster.master_data_mgr.dialogs.PositionDlg;
import org.softcaster.master_data_mgr.models.MasterDataTableModel;
import org.softcaster.master_data_mgr.models.beans.PositionBean;
import org.softcaster.master_data_mgr.ui.ZebraTable;

/**
 *
 * @author softc
 */
public class PositionPanel extends AbstactMDPanel {
    
    private final MasterDataFacade masterDataFacade;

    /**
     * Creates new form ForexPanel
     *
     * @param masterDataFacade
     */
    public PositionPanel(MasterDataFacade masterDataFacade) {
        this.masterDataFacade = masterDataFacade;
        initComponents();
        postInitComponents(positionTable);
    }

    /**
     * This method is called from within the constructor to initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is always
     * regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        popUp = new javax.swing.JPopupMenu();
        acNew = new javax.swing.JMenuItem();
        acMod = new javax.swing.JMenuItem();
        acDel = new javax.swing.JMenuItem();
        lblHeader = new javax.swing.JLabel();
        scrollPane = new javax.swing.JScrollPane();
        positionTable = new ZebraTable();

        acNew.setIcon(new javax.swing.ImageIcon(getClass().getResource("/images/angular/draft_16dp.png"))); // NOI18N
        acNew.setText("New");
        popUp.add(acNew);

        acMod.setIcon(new javax.swing.ImageIcon(getClass().getResource("/images/angular/edit_16dp.png"))); // NOI18N
        acMod.setText("Edit");
        popUp.add(acMod);

        acDel.setIcon(new javax.swing.ImageIcon(getClass().getResource("/images/angular/delete_16dp.png"))); // NOI18N
        acDel.setText("Delete");
        popUp.add(acDel);

        setBackground(new java.awt.Color(255, 255, 255));
        setBorder(javax.swing.BorderFactory.createEmptyBorder(20, 20, 20, 20));
        setLayout(new java.awt.BorderLayout(10, 10));

        lblHeader.setFont(new java.awt.Font("Segoe UI", 1, 18)); // NOI18N
        lblHeader.setForeground(new java.awt.Color(50, 50, 50));
        lblHeader.setHorizontalAlignment(javax.swing.SwingConstants.CENTER);
        lblHeader.setText("Positions Master Data");
        lblHeader.setVerticalAlignment(javax.swing.SwingConstants.TOP);
        add(lblHeader, java.awt.BorderLayout.NORTH);

        positionTable.setModel(new javax.swing.table.DefaultTableModel(
            new Object [][] {
                {null, null, null, null},
                {null, null, null, null},
                {null, null, null, null},
                {null, null, null, null}
            },
            new String [] {
                "Title 1", "Title 2", "Title 3", "Title 4"
            }
        ));
        scrollPane.setViewportView(positionTable);

        add(scrollPane, java.awt.BorderLayout.CENTER);
    }// </editor-fold>//GEN-END:initComponents


    // Variables declaration - do not modify//GEN-BEGIN:variables
    private javax.swing.JMenuItem acDel;
    private javax.swing.JMenuItem acMod;
    private javax.swing.JMenuItem acNew;
    private javax.swing.JLabel lblHeader;
    private javax.swing.JPopupMenu popUp;
    private javax.swing.JTable positionTable;
    private javax.swing.JScrollPane scrollPane;
    // End of variables declaration//GEN-END:variables

    @Override
    protected void fillModelList() {
        // Crea e setta il model
        PositionBean prototype = new PositionBean(null);
        MasterDataTableModel<PositionBean> model = new MasterDataTableModel<>(prototype);
        positionTable.setModel(model);

        // Popola il model
        refreshModel(model);
    }
    
    @Override
    protected void acNewActionPerformed(ActionEvent evt) {
        java.awt.Window parentWindow = javax.swing.SwingUtilities.getWindowAncestor(this);
        java.awt.Frame parentFrame = null;
        
        if (parentWindow instanceof java.awt.Frame frame) {
            parentFrame = frame;
        }
        
        PositionDlg dialog = new PositionDlg(parentFrame, true, null, masterDataFacade);
        dialog.setSize(600, 400);
        // Centra la dialog rispetto al pannello
        dialog.setLocationRelativeTo(this);
        dialog.setVisible(true);

        // Post chiusura dialog
        MasterDataTableModel<PositionBean> model = (MasterDataTableModel<PositionBean>) positionTable.getModel();
        refreshModel(model);
    }
    
    @Override
    protected void acModActionPerformed(ActionEvent evt) {
        int rowIndex = positionTable.getSelectedRow();
        if (rowIndex != -1) {
            // 1. CONVERSIONE FONDAMENTALE
            int modelRow = positionTable.convertRowIndexToModel(rowIndex);
            MasterDataTableModel<PositionBean> model = (MasterDataTableModel<PositionBean>) positionTable.getModel();
            PositionBean positionoBean = model.getElementAt(modelRow);
            java.awt.Window parentWindow = javax.swing.SwingUtilities.getWindowAncestor(this);
            java.awt.Frame parentFrame = null;
            
            if (parentWindow instanceof java.awt.Frame frame) {
                parentFrame = frame;
            }
            
            PositionDlg dialog = new PositionDlg(parentFrame, true, positionoBean, masterDataFacade);
            dialog.setSize(600, 400);
            // Centra la dialog rispetto al pannello
            dialog.setLocationRelativeTo(this);
            dialog.setVisible(true);

            // Post chiusura dialog
            refreshModel(model);
        }
    }
    
    @Override
    protected void acDelActionPerformed(ActionEvent evt) {
        int rowIndex = positionTable.getSelectedRow();
        if (rowIndex != -1) {
            // 1. CONVERSIONE FONDAMENTALE
            int modelRow = positionTable.convertRowIndexToModel(rowIndex);
            MasterDataTableModel<PositionBean> model = (MasterDataTableModel<PositionBean>) positionTable.getModel();
            PositionBean bean = model.getElementAt(modelRow);
            if (JOptionPane.showConfirmDialog(this,
                    "Are you sure to delete item? " + bean.getValueAt(0), JMasterDataMgr.TITLE,
                    JOptionPane.YES_NO_OPTION,
                    JOptionPane.QUESTION_MESSAGE) == JOptionPane.YES_OPTION) {
                try {
                    masterDataFacade.getPositionMasterDataDAO().delete(bean.getPositionMasterData());
                    refreshModel(model);
                } catch (Exception ex) {
                    LoggerMgr.logError(ex.getLocalizedMessage());
                }
            }
        }
    }
    
    @Override
    protected void refreshModel(MasterDataTableModel model) {
        List<PositionMasterData> positions = masterDataFacade.getPositionMasterDataDAO().findAll();
        List<PositionBean> positionBeanList = new ArrayList<>();
        PositionBean bean = null;
        for (PositionMasterData item : positions) {
            bean = new PositionBean(item);
            positionBeanList.add(bean);
        }
        model.setData(positionBeanList);
    }
    
    @Override
    public void downloadAction() {
        System.out.println("CurrPairPanel downloadAction");
    }

    @Override
    public void filterAction() {
    }
}
